cotton.draw(function(context) {
	context.beginPath();
    context.rect(10,10,10,10);
    context.fillStyle = '#8ED6FF';
    context.fill();
    context.lineWidth = 2;
    context.strokeStyle = 'black';
    context.stroke();
})
